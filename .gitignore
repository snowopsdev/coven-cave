# Dependencies
node_modules

# Build output
.next
out
src-tauri/target
src-tauri/gen
tsconfig.tsbuildinfo
next-env.d.ts

# Tauri sidecar bundle (generated by scripts/sidecar-bundle.sh)
src-tauri/resources/server/

# Notarized release artifacts
release/

# Local Playwright verification outputs (e.g. scripts/verify-trace-graph-3d.mjs)
artifacts/

# Logs
*.log

# OS junk
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.zed/

# Agent / tool scratch state (local automation, not for sharing)
.claude/
.cursor/
.aider*
.comux*

# Environment files (bare + scoped). Per-machine overrides via
# .env*.local already covered, and *.example files stay tracked.
.env
.env.*
!.env.example
!.env.*.example

# Secrets & credentials — defense in depth alongside the pre-commit hook
*.pem
*.key
*.p12
*.pfx
*.p8
*.cer
*.crt
*.der
*.jks
*.keystore
*.mobileprovision
*.provisionprofile
id_rsa
id_ed25519
id_ecdsa
id_dsa
secrets/
credentials/
.netrc

# Git worktrees
.worktrees/
.env.local

# Playwright debug artifacts (console/page dumps from headless runs)
.playwright-cli/

# npm lockfile — project uses pnpm
package-lock.json
/.superpowers
/docs/superpowers
